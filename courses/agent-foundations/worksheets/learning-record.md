# 我的第一章学习记录

这是空白模板，不代表已经学习或通过。`new-workspace` 会将它复制到你的本地工作区。
不要把密钥、真实账户数据或个人图书正文填入这里。修改时追加一段，不覆盖首次判断。

## 基本信息

- 学习开始日期：
- 课程版本：1.0.0
- 讲义修订：2026-09-07.tutorial-1
- 使用的代码版本 / 自己保存的快照：
- Python 与依赖环境：
- 我的编程经历（自述，不作为能力结论）：
- 本次可用时间：
- 下一次复测日期：
- 我是否参与过出题或查看过参考实现 / 场景源码：

帮助记录统一使用：`H0` 无提示；`H1` 定位提示；`H2` 结构提示；`H3` 局部示范；
`S` 完整解答或直接生成实现。可以写多个，例如“H1 后仍卡住，使用 H2”。
使用 AI 不需要隐瞒，记录它解释、定位或代写了哪部分即可。
桌面可将当前步骤的记录项写进“预测与作答”，不用在磁盘另建这个文件。阅读概念背景、
跟随已解示例、获得本题控制结构、查看完整解答应分别记录；不要把照着示范的成功当作
独立尝试。表中的 CLI 路径在桌面可改填历史记录标识和导出备份位置。

## 第一步：先修诊断

### 首次作答（查看提示前）

- A：first / second / state；加入局部重新赋值之后；理由：
- B：events；continue 与 break 的区别；静默吞错误的影响：
- C：嵌套读取表达式；参数 42 的问题：
- 花费时间与帮助级别：

### 对照与补练

- 具体错在哪里：
- 暂时判断是 Python 基础缺口还是题意不清；证据：
- 只补哪一个小练习：
- D2、D2 变式的首次答案及理由：
- 补练后仍不确定什么：

## 第二步：解释轨迹

- 按三次模型决策，分别说明请求、实际执行和结果回填：
- 预测模型决策 / 工具执行次数：
- 正常参考运行记录（桌面可先填纸面推演，第三步再补实际运行记录）：
- 实际计数与差异：
- 搜索标题为什么不能替代正文：
- 被拒绝的调用怎样让模型知道：
- 预算已满时谁决定停止：
- 帮助级别与用自己的话解释的原文：

## 第三步：手搓首次实现

- 运行前预测：
- 我自己写了什么，外部帮助了什么：
- 消息、工具结果、最终退出分别在哪段代码：
- 首次运行命令及运行文件：
- 报告中的 code_sha256 / code_snapshot_directory：
- 首次状态是 incomplete / error / completed / budget_exhausted：
- 我的实现怎样取得答案，如何排除硬编码：

运行报告的 `learning.status=pending_review` 表示教学证据仍待评阅。即使运行判定
通过，也不要把它手工解释为系统已经确认独立掌握。

### 保存快照后的独立重写

- 原实现快照路径、对应运行文件与 code_sha256：
- 重写副本路径（保留原实现）：
- 保留了哪些 helper，清空了哪段核心循环：
- 开始时是否关闭原实现、参考解答、提示与 AI 对话：
- 写代码前预测的回填顺序与继续/停止理由：
- 未经提示的首次代码快照、完成或卡住的位置：
- 首次尝试开始 / 结束时间及用时：
- 首次运行文件与结果：
- 查阅了哪些语法/API 文档；是否得到关键策略提示：
- 如需帮助，提示级别、提供时间和具体内容：
- 提示后另一次修改的快照、运行文件及结果：
- 重写前后差异；一个遗漏或差点遗漏的状态变化：
- 当前支持独立重建 / 仅支持辅助完成 / 待核验；依据：

## 第四步：单次故障实验记录（每个有意义的尝试复制一份）

- 日期 / 场景 / 实现 / 代码快照：
- 修改前预测（先填）：
- 实际运行文件与最早偏离预测的事件：
- 两个可能原因：
- 本次选择验证哪个原因，为什么：
- 做了哪一个改动：
- 修改后运行文件及结果：
- 哪条证据支持或推翻了原假设：
- 帮助级别 / AI 提供的具体帮助：
- 当前仍不确定什么：

## 第五步：同题配对

填写“待运行 / 通过 / 未通过”，并给出运行文件。此表只记录作品门禁。

| 场景 | 自己的 manual | 自己的 LangGraph | 运行文件 / 差异与原因 |
| --- | --- | --- | --- |
| normal | 待运行 | 待运行 | |
| invalid_call | 待运行 | 待运行 | |
| invalid_arguments | 待运行 | 待运行 | |
| transient | 待运行 | 待运行 | |
| missing | 待运行 | 待运行 | |
| transfer（公开练习） | 待运行 | 待运行 | |
| model_budget | 待运行 | 待运行 | |
| tool_budget | 待运行 | 待运行 | |

- 我画出的图和状态说明：
- 两版的答案、引用、调用、预算和退出原因如何比较：
- 一个消息更新风险：
- 框架负责什么，宿主仍负责什么：
- 对这个任务的实现选择及改变选择的条件（150 字以内）：

## 第六步：迁移与复测

### 公开迁移练习

- 是否已读场景源码、规格或答案：
- 运行前的三项判断：
- 首次运行文件与结果：
- 如需修复，假设、验证、帮助及第二次结果：
- 三个反事实追问的原始答案：

### 另外安排的未见变式

- 出题者及题目全文：
- 与已见场景相比改变了什么：
- 是否确实未见 / 是否自己参与设计：
- 第一次预测、轨迹与解释（先写，后看反馈）：
- 时间 / 帮助级别：
- 评分依据；仍不能证明的能力：

### 延迟复测

- 计划日期 / 实际日期：
- 复测前是否复习了答案：
- 不看原实现时的首次轨迹、计数和回答边界：
- 回到代码后发现的对应点或不一致：
- 帮助、修正与复验记录：
- 稳定性证据是支持 / 不足 / 与之前矛盾；理由：

## 可选真实模型冒烟（与上述门禁分开）

- 实际时间、模型名、端点地址（不得含密钥）：
- 是否明确允许远程发送本章模拟任务：
- 命令（无密钥）与运行文件：
- 耗时与结果；如无真实用量数据，不估算成确定费用：
- 与 scripted 不同的行为：
- 未执行则写“未执行”：

## 本章当前证据

| 维度 | 0 / 1 / 2 / 待核验 | 原始作答或运行依据 | 评阅者 |
| --- | --- | --- | --- |
| 解释循环及职责 | 待核验 | | |
| 预测状态变化 | 待核验 | | |
| 诊断及恢复策略 | 待核验 | | |
| 未见场景迁移 | 待核验 | | |

- 作品门禁：待核验
- 学习状态：未验证 / 辅助完成 / 独立通过 / 待核验（选一项并给证据）
- 延迟稳定性：待复测
- 当前最值得练的一项：
- 下一项任务，以及它对应的本次证据：
