# ROADMAP

本文件记录当前源码中的能力和仍未完成的验收/边界。勾选表示已有实现与自动化覆盖，
不等同于已在所有真实文档和外部软件环境中完成 GUI 验收。

## 已实现

- [x] 统一文档模型
  - [x] 稳定 ID、revision、`BookDocument`、线性 `ContentUnit`
  - [x] 独立层级 `TocNode` 与可复用 `DocumentLocator`
  - [x] 段落、标题、列表、引用、代码、表格、图片、音频、视频和受限 RawHtml 块
  - [x] Markdown/HTML 解析、清洗、AST 与规范化序列化
- [x] 本地持久化
  - [x] SQLite 当前结构及按表拆分的 CRUD/查询映射
  - [x] 开发期不迁移：结构或数据关系不一致时重建数据库与受管对象目录
  - [x] 基于 `object_store` LocalFileSystem 的 BLAKE3 内容寻址 `BlobStore`
  - [x] 原件、封面、媒体与派生页面外置；先写对象再事务发布引用
  - [x] 删除先解除引用、后台回收对象及启动孤儿 GC
  - [x] 删除图书后关闭该书已打开的阅读、PDF/Office 预览和编辑窗口；未保存修改随图书
    丢弃，不再走保存确认，也不再写入最终阅读进度
  - [x] 媒体归属校验、MIME、单 Range 与 `206` 响应
  - [x] 单个应用进程内由共享 `AppServices` 为多窗口 mutation 排序并发布单调投影
- [x] 导入、阅读与预览
  - [x] EPUB 2/3
  - [x] PDF（本地固定 PDF.js，禁用 JavaScript/XFA/联网；导入与逐页视觉共享
    256 MiB、4,096 页上限）
  - [x] DOC/DOCX、PPTX、XLSX（`office_oxide` 转统一模型）
  - [x] DRM-free MOBI/AZW/AZW3（`ebook-rs`，加密内容明确拒绝）
  - [x] 所有导入格式保留字节一致的原文件
  - [x] Office/Kindle 结构化预览与 PDF 固定版式查看
  - [x] 用户按书显式启用的 Office COM 增强预览：Word/Excel 经临时 PDF 光栅化且页面
    不猜测内容单元归属，PowerPoint 逐页图片保留精确幻灯片映射；最终页面按
    renderer/revision/profile 持久化到对象存储
  - [x] Office 原件只读、禁宏/外链更新、不主动调用宏/OLE、可取消，失败或未安装时
    回退结构化预览（仅建议对可信文件启用，不把 Office 解析视为安全沙箱）
  - [x] EPUB/PDF 阅读器和编辑器使用可拖动的三栏布局，左右栏分别限制在可用范围内，
    窗口缩小时保留中央正文空间且不破坏 AI 侧栏的自动收起行为
- [x] 新建与编辑
  - [x] 新建图书先要求用户输入非空名称，确认后创建默认包含 Markdown 章节的图书
  - [x] 编辑元数据、封面、内容单元类型及 Markdown/HTML 正文
  - [x] 新增、删除、重排内容单元，目录嵌套、缩进与提升
  - [x] 编辑器显示独立目录树的全部节点，按稳定目标 ID 选择正文；同章子目录独立
    选中，保存正文保留各项标题；未列入目录的内容单独列出
  - [x] 本地固定 ProseMirror 富文本编辑器及快照屏障
  - [x] 编辑器从规范化 AST 生成合法 XHTML，统一处理图片、换行、媒体布尔属性和
    受限 HTML；已有 HTML 正文直接可用，不改变持久化源码格式
  - [x] 关闭编辑窗口提供保存并关闭、不保存和取消三个选项；确认后才保存，保存失败
    保留窗口；等待已接受的保存/导出及 WebView 初始化完成后再询问
  - [x] 图片、音频、视频块的插入、替换、删除与本地播放
  - [x] 媒体标题/人工说明编辑及全文索引
  - [x] 编辑器只从统一 AST 生成安全预览，持久媒体按授权 ID 和 Range 异步读取
  - [x] 受信编辑器 shell 与不可信规范化正文使用不同 origin，IPC 只接受完整页面身份
- [x] 导出
  - [x] 从统一模型稳定导出 EPUB
  - [x] 从统一模型稳定导出 PDF
  - [x] 导入图书可导出字节一致的原件
  - [x] 统一模型可规范化导出 EPUB/PDF，不承诺复刻原 Office/PDF/Kindle 复杂版式
  - [x] 同目录临时文件同步后原子替换目标
- [x] 搜索与派生索引
  - [x] 稳定 chunk、FTS5 关键词搜索和图书范围硬过滤
  - [x] 静态集成 `sqlite-vec` 的精确 KNN
  - [x] 关键词、语义与 RRF 混合模式；向量不可用时回退 FTS
  - [x] 持久化 embedding、视觉渲染和 vision 队列及 revision 失效处理
  - [x] 后台任务暂停、恢复、重试、取消及进程重启恢复；视觉渲染逐页持久化断点，
    恢复时跳过已提交页面，完整页面集与成功状态原子发布
  - [x] 后台任务默认不自动运行：新导入、创建或保存编辑仍持久创建
    `visual_render`、`vision`、`embedding` 任务，但初始为已暂停且不立即执行，可在后台
    任务窗口逐项恢复；开启“自动运行新创建的后台任务”后新任务才直接排队执行；设置变化
    不打断或改变此前已排队、正在运行的任务
  - [x] 每个图书的当前来源只保留一个有效视觉理解任务；AI 配置未变化时不重复投递，
    视觉模型变化时替换旧任务；遗留重复/来源不明任务会清理旧派生结果后重建，且 AI
    配置与全部当前来源的索引契约原子切换、失败整体回滚
  - [x] embedding 重配置按规范化端点与 embedding 模型判定执行身份；聊天/视觉模型、
    超时和 API key 单独变化不重置已完成任务，embedding 模型或端点变化只首次投递；
    游标代次保护会清理旧向量并拒绝旧 provider 的迟到写入，同时保留视觉理解完成后的
    独立增量 embedding 任务
  - [x] 逻辑页面的实际 PNG 光栅化，以及带归一化页面区域的视觉说明/OCR chunk；
    Word/Excel 整本 PDF 无法证明页到单元映射时仅保留增强预览页，不生成视觉索引
- [x] OpenAI-compatible AI Agent
  - [x] 默认本地 Ollama `http://127.0.0.1:11434/v1`
  - [x] 低资源开发默认模型：聊天/视觉 `qwen3.5:0.8b`，embedding
    `qwen3-embedding:0.6b`，仍可在设置中分别修改
  - [x] models、流式 chat completions/tools/vision 与 embeddings；正文按 SSE 增量临时
    展示，tool call、取消、流错误或来源校验失败时撤回，校验成功后才提交最终回答
  - [x] 问答在明确的上下文超限响应后有限重试，优先移除旧历史整轮，再减少检索结果
    整条片段并同步收紧有效引用；保留当前问题、冻结选区、工具配对和每个结果的至少
    一条完整片段。必要内容仍超限时提示调整输入或服务端容量，不自动更改 Ollama 配置
  - [x] 识别 Ollama 在回答流建立前返回的工具参数 JSON 不完整错误，每次问答最多纠正
    重试一次；保留已有工具结果、范围与来源校验，持续失败时提示检查模型及服务设置
  - [x] 聊天、embedding、vision 模型分别配置，可设置 1–600 秒请求总超时；远程/非
    HTTPS 显式确认且绑定准确端点
  - [x] AI 设置窗口为应用级单例，重复打开时激活并恢复现有窗口，关闭后可重新创建
  - [x] AI 设置按 Endpoint、对话模型、联网搜索、后台任务分为四个标签；切换保留未保存
    输入，各页独立滚动，统一保存全部配置；模型检测在对话模型页展示结果
  - [x] 对话模型页支持 Temperature、Top P、Presence/Frequency penalty 和最大输出
    token（正整数，默认 4096，由用户按模型设置，不设固定上限）；可选参数留空使用
    服务默认值，支持恢复默认后统一保存。参数共用
    一份配置，保存后用于后续新问答及其联网后备回答，不改变进行中的问答或视觉理解
  - [x] API key 存 Windows Credential Manager
  - [x] 只读 `search_books`、`read_passages`、`get_outline` 工具和宿主范围硬过滤
  - [x] 图书库、Reader、PDF Reader、Editor 可折叠 AI 侧栏
  - [x] 每个窗口上下文可新建、列出和切换多个独立持久会话；空会话首次发送时才创建，
    切换时恢复对应消息和来源，流式请求期间阻止会话竞态
  - [x] 会话经逐项确认后可删除，其问题、回答和引用同步永久移除；删除当前或最后一个
    会话后回到空白新会话，删除非当前会话保留当前内容，跨窗口正在使用的会话拒绝删除
  - [x] 用户与 AI 历史消息支持文字选择和逐条原文复制；消息轨道占满侧栏可用宽度，
    长消息使用稳定最大宽度
  - [x] AI 流式回复和历史回答支持 Markdown 标题、强调、列表、引用、代码块和表格；
    保留原文复制，图片和正文链接仅展示文字，来源继续通过宿主校验后的引用按钮打开
  - [x] Office 增强页面窗口复用当前书 AI 范围；PPTX 逐张幻灯片保留精确 locator，
    Word/Excel 整本 PDF 页（即使页数与单元数相同）仅标记派生页码、仅供预览且不提供引用
  - [x] Library 当前分组后代范围；Reader/Editor 当前书必选并可追加其它书
  - [x] Reader/PDF Reader/Editor 的非空当前高亮默认加入本轮且可在右栏取消；清空高亮
    不会退化为整章引用，额外章节/页面仍显式多选，无选区时不向模型发送内部空快照数组
  - [x] “可选引用”列表最多显示三行，更多候选保留在列表内滚动访问，不挤占聊天记录区域
  - [x] 高亮、编辑器未保存选区及多章节使用 revision 精确快照；请求在异步冻结前即可
    取消，恢复时无法由当前 AST 精确证明的未保存选区标记为失效
  - [x] 持久会话、流式取消、受 scope/locator/document revision/unit revision
    校验的来源标记及可点击引用；Reader/Editor 重新校验 AST 并只定位唯一文字范围，
    伪造、越权、旧版本、缺失或歧义来源明确报错且不降级跳转
  - [x] 所选图书没有可验证来源时允许模型基于通用能力回答，并在答案开头明确标为
    “非知识库回答”

## 验收状态（2026-09-03）

以下勾选只代表对应门禁已执行，不会把仍未完成的真实 GUI、外部软件或安全语料验收
折叠成“整体已完成”。

- [x] 在 Windows/MSVC 上以仓库外九格式语料运行完整 `format_corpus_gate`：EPUB、PDF、
  DOC、DOCX、PPTX、XLSX、MOBI、AZW、AZW3 均完成导入、规范化编辑、FTS 搜索、
  EPUB/PDF/原件导出、原件字节校验及重新打开
- [ ] 扩充真实复杂语料：中文字体、公式、复杂/合并表格、旋转/扫描 PDF、PPTX
  母版/图表、隐藏工作表区域，以及损坏、加密和压缩炸弹文件
- [ ] 在安装与未安装 Microsoft Office 的 Windows 环境完成真实 GUI 增强预览验收，
  覆盖持久化页面重开、禁用、超时、取消和结构化回退
- [ ] 用隔离数据目录走完多格式导入 → 查看 → 编辑 → 保存 → 三种搜索 → AI 多会话
  新建/切换/重启恢复、消息选择/复制与引用 → EPUB/PDF/原件导出 → 重新打开的 GUI 矩阵
- [ ] GUI 覆盖 Ollama 离线/模型缺失、媒体 seek，以及 WebView 构建中和构建后关闭窗口
- [ ] 在真实 Windows Credential Manager 完成 API key 保存、读取、删除及失败路径验收；
  当前已实现平台存储并有依赖注入测试，但不能用内存 mock 代替真机凭据验收

## Agent 第一章独立实验包（2026-09-07）

入口为 `courses/agent-foundations/README.md`，使用 Python 3.12.10 与精确锁定的
LangGraph 依赖。CLI 课程验证与桌面接入的验证分别列出。

- [x] 中文讲义、先修诊断、两种代码骨架、独立参考解答、评分量规、学习记录和
  试学观察表；六份参考资料映射到本章能力与阅读位置
- [x] 手搓循环与真实 LangGraph `StateGraph` 实现，统一运行协议、两种只读工具、
  虚构资料、模型决策/实际执行预算、受限重试与明确停止原因
- [x] 固定响应回填检查与 HTTP mock 覆盖；来源按宿主成功读取记录判定，学生日志
  不参与作品评分；预算结束同样检查轨迹，作品通过不会自动判断独立掌握
- [x] 实际 CLI 完成两版 × 八场景对照；测试覆盖学习者工作区、未完成骨架、错误
  定位、代码快照摘要、逐项检查、调用量及模型协议边界；Ruff 格式与静态检查通过
- [x] 运行报告、完整代码快照和对照结果保存在忽略的 `runs/`，学习记录保存在忽略
  的 `workspaces/`；不访问真实图书库或修改墨页数据库
- [x] 已分别运行两版本机 Ollama `qwen3.5:0.8b`、`ornith-1.5:9b` 冒烟并保留实际
  成功与失败记录：包括读到正确事实却添加自然语言前言、未返回规定 JSON、输出
  截断及请求超时；显式配置 Ollama 的 token 字段、JSON 模式并明确输出提示后，
  9B 最新同题对照两版均通过：手搓为 3 次模型决策 / 3 次工具执行，LangGraph 为
  3 / 4。此前同配置仍有格式失败，不能以单组成功宣称模型输出稳定
- [ ] 扩展真实模型多场景与重复运行验收，继续比较调用顺序、格式错误和耗时差异
- [ ] 开发者本人实际跟练并修订文案，再由未参与设计的目标学习者验证首次体验；
  当前试学表为空白待执行，自动 CLI 测试不代替真人试学

## 第一章学习中心接入（2026-09-07）

- [x] 连续教程修订 `2026-09-07.tutorial-1`：第一章六节围绕同一研究任务展开，正文提供
  数据与消息演算、完整正常版 / 重试版 / LangGraph 版和就地自查，无需先读外部资料。
  新增第2—10章工具契约、检索、记忆、规划、评测、审批、多 Agent、MCP 与综合项目，
  各章配手搓 / 库版本的离线示例；学习支架与独立尝试分开，真人首次试学仍待执行

- [x] 图书库左侧学习入口、原生三栏窗口、六步讲义、手搓 / LangGraph 代码编辑、
  预测与作答；固定响应场景选择、运行 / 取消、结果、轨迹、当次快照与历史接线
- [x] 默认打开完整教程总览，左侧可访问十章、第一章六步及五份实验资料。
  提供上一章 / 下一章、上一节 / 下一节及第六节进入第二章；开始 / 继续第一章
  进入保留步骤，十章浏览不重排已有六步索引，也不替换代码或自动登记通过
- [x] Python 语法显式注册；手搓、LangGraph 编辑区及只读代码按 Python 着色，覆盖
  关键字、函数、字符串、数字和注释；颜色映射与编辑后重新解析有回归测试
- [x] 讲义、课程资料、参考代码、作答快照和轨迹详情改用外层滚动，正文与选区一起
  移动，避免内部虚拟列表滚动后把其他文字重新算入选区。三个 GPUI 事件回归覆盖
  单行、多行和代码块的滚动、复制与重选；同一测试已复现旧实现错误，用户于
  2026-09-07 实测反馈已修复
- [x] 学习记录独立 JSON 存储；保存前 revision 校验、原子替换、运行前冻结首次
  代码和预测、完成报告不可覆盖；导出、恢复、新一轮归档，损坏当前档案可从有效
  备份恢复并保留原始坏文件。导入记录标为待核验
- [x] 受信 Python 宿主持有模型 / 工具观察和评分；学生代码在 Windows LPAC + Job
  中执行，拒绝普通 AppContainer；不授予网络、宿主文件或子进程访问，不允许写入
  暂存课程和本次 profile 文件 / 注册表；限制 CPU、内存、期限、输出和 RPC
- [x] 独立 Windows 底层十项门禁通过，覆盖文件与网络拒绝、私有 profile 禁写、
  内存 / CPU / 强制终止、普通 AppContainer 拒绝、宿主死亡与恢复清理
- [x] 隔离 RPC 全部八场景 × 两版通过；真实 asyncio 导入与同步 StateGraph 可用，
  原生 IOCP 和网络访问仍被拒绝。工作进程的初始化适配不修改已安装 Python 或依赖
- [x] Rust 完整服务 → JSONL 宿主 → 隔离工作进程，两版运行和学习档案导出 / 恢复
  专项通过；另三项宿主控制流测试验证管道阻塞取消、异常退出和报告后挂起均不误判通过
- [x] 首轮常规门禁：Python 358 项、Rust 全目标 515 项通过（包含 Python 高亮及导读内容回归）；另显式运行上述四项
  Rust 专项。格式、全目标编译和 Clippy 通过，触及范围无新增告警；未运行与本次
  改动无关的外部真实格式语料门禁
- [x] 连续教程验证：`uv sync --locked`、Ruff 格式 / 检查、pytest 381 项通过；第一章
  八场景 × 两版通过，第2—10章九组示例输出一致。63 个 Python 正文代码块编译、
  12 个 JSON 块解析通过，主要示范与练习变式经实际运行复核
- [x] 连续教程最终 Rust 验证：`cargo fmt --all --check`、
  `cargo check --all-targets --locked`、`cargo test --all-targets --locked --no-fail-fast`、
  `cargo clippy --all-targets --locked` 与产品构建通过。全目标 528 项通过
  （lib 356、产品 158、集成 14）。另以 `cargo test --lib --locked learning:: -- --ignored
  --test-threads=1` 显式运行 4 项服务 / 宿主专项，全部通过；外部真实语料门禁未重跑。
  Clippy 保留既有告警；最终全目标运行已解除此前 exe 占用的阻塞
- [x] 在唯一隔离数据目录实际点击验证：图书库 → 教程总览 → 第一章、上下节 / 章节
  切换、第十章末尾、代码高亮、返回总览继续原位置、关闭后重新打开继续，以及最终
  关闭全部测试窗口。学习位置已落盘，测试进程退出，错误日志为空
- [x] 修复自测复现的 Office 开关竞态：确认完整提交身份后允许任务已 Running /
  Succeeded；缺失、身份变化、停止或队列关闭仍报错，不重置任务。FakeOffice 与
  临时数据库的门控回归覆盖迟到确认，另覆盖身份和队列错误；未新增真实 Office GUI 验收
- [x] 使用唯一隔离目录走完真实 Windows 学习实验：编辑 / 复制参考代码、预测、
  手搓正常场景（3 次决策 / 3 次执行）、LangGraph 超时重试（3 / 4）、正文轨迹、
  首次快照和历史加载。运行后编辑不被旧报告覆盖，帮助级别保留为完整示范
- [x] 导出包含未保存笔记，取消选择保留输入；恢复前将当前代码、未保存笔记及
  全部 5 次尝试归档，恢复的结果和逐项检查标记为待核验 / 原记录。修复实际点击
  发现的 Windows `\\?\` 路径导致恢复对话框打不开，以及该错误误锁编辑区的问题
- [x] 实际按钮取消和原生关闭期间取消均保存 cancelled 报告，工作进程已执行标记
  可在报告中核对；关闭书库保留学习窗口，再关闭最后窗口时等待停止和保存。
  另一个持续循环的工作进程终止后不误判通过；重启保留代码、预测和学习步骤。
  全部测试窗口已关闭，应用 / 工作进程退出，`RUST_LOG=error` 日志为空
- [ ] 桌面真实模型运行配置和真人首次体验验证；桌面为同步 Python 与虚构练习，
  第一章使用固定响应模型，后续章节读取运行时练习输入，不自动判断专家水平

## 第2—10章桌面实验（2026-09-07）

- [x] 章节正文提供“本章代码实验”，进入任务说明、Python 手搓 / 库实现、预测与作答。
  九章各有两份提交骨架与两份参考实现，沿用同步五参数 `run`、运行 / 取消、
  宿主轨迹与检查、当次代码快照和历史；“本章正文”可返回对应教程
- [x] 各章保存独立代码、预测、提示级别、场景和报告。切换实验前异步保存旧章，
  保存失败保留输入；备份绑定章节，拒绝跨章恢复。第一章六步和已有档案保持可读
- [x] 损坏章节可进入自身恢复状态或离开，不阻断其余章节；切章、恢复和新一轮隔离
  四个编辑区的撤销历史，普通保存保留撤销。GPUI 真实按键回归覆盖 Ctrl+Z / 重做，
  学习窗口 23 项事件及内容检查通过
- [x] 每章提供正常、故障和迁移场景，宿主生成虚构资料并检查实际动作；工作进程不含
  评分器、私有数据或参考实现。第2—10章将输入获取与真实模型推理明确区分，
  学员打印、事件和通过声明不能替代宿主记录；作品通过不登记为独立掌握
- [x] 工具契约、正文证据、记忆过滤、计划预算、评测指标、审批摘要、协作权限、
  MCP 握手与综合项目都有独立行为检查。审批、草稿写入和 MCP 是受控模拟，
  不连接外部服务，不等同于真实写入或联网协议验收
- [x] 修复固定 ID 泄露判断类别的练习生成问题：编号不透明，审批、记忆、评分样本、
  协作声明和候选答案具有运行时变式；测试拒绝按场景、编号或排序位置猜结果
- [x] 当前工作区 Rust 格式、全目标编译与 Clippy 检查通过，学习改动无新增 Clippy
  告警。使用 `cargo test --target x86_64-pc-windows-msvc --all-targets --locked
  --no-fail-fast` 完成独立 Windows 输出的 559 项测试；另显式运行 5 项宿主 / LPAC
  专项全部通过。独立输出避开当时正在运行的默认 Debug exe；外部真实格式语料未重跑
- [x] 默认 Debug exe 占用解除后，`cargo build --locked --bin moye-epub-editor` 与
  `cargo test --all-targets --locked --no-fail-fast` 再次通过（同为 559 项），默认运行入口
  已包含新章节实验
- [x] Python 全量 598 项通过；最后的编号与内容变式补强后，受影响章节的 239 项全部
  复验通过，其中包含九章 × 三场景 × 两实现的真实 LPAC 矩阵及伪造、越权、预算、
  取消反例。uv 锁定安装、Ruff 格式与检查通过；第一章八场景双实现及九组本地示例
  对照通过。正文 78 个 Python 块、23 个 JSON 块与 30 个本地链接检查通过
- [ ] 本轮新章节的真实 Windows 鼠标操作复验：自动化工具 native pipe 连接失败，
  重试与重置仍不可用。事件回归和真实 LPAC 执行不替代此项验收

## 明确不在当前范围

- [ ] S3 对象存储实现、本地/远程同步和多设备冲突处理
- [ ] DRM Kindle 导入或 DRM 绕过
- [ ] 修改后写回 DOC/DOCX、PPTX、XLSX、MOBI/AZW/AZW3
- [ ] EPUB 导入时将章节内 fragment 映射为精确块目标；当前编辑器子目录打开所属章节
- [ ] 原 Office/PDF/Kindle 复杂版式的无损规范化导出
- [ ] Office 宏/OLE 主动调用、PDF/书内脚本或外部网络资源
- [ ] 同一数据目录的多应用进程并发、跨进程 mutation 排序或单实例锁；当前仅支持一个
  应用进程，同进程多窗口由共享服务排序
- [ ] 音频/视频自动转写
- [ ] AI 写书或其它写操作工具
- [ ] 在许可证元数据冲突解决前进行打包、发布或重新分发
- [ ] 非 Windows 平台的正式支持声明
